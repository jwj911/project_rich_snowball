# R7 验收清单

- [x] 生产发布预检覆盖 `ENV`、PostgreSQL、强密钥、HTTPS CORS、真实数据源、Redis 和发布元数据。
- [x] `SSE_DEPLOYMENT_MODE` 在生产环境必填，且只接受 `single` 或 `sticky`。
- [x] 合法输入返回退出码 0；缺失或不安全输入返回非 0 和稳定检查代码。
- [x] 每次预检生成独立 `trace_id` 和结构化 JSON 诊断报告。
- [x] stdout、stderr、日志和报告均不包含原始密钥、数据库/Redis 密码、Provider Token 或行情数据。
- [x] 预检为只读操作，不修改数据库、Redis、部署状态或发布清单。
- [x] worker 成功刷新 realtime quotes 后会更新本地及 Redis 共享标记。
- [x] API 进程能够感知另一个进程写入的共享标记，并在下一个检查周期触发 SSE 推送。
- [x] 行情刷新失败时不会写入共享更新标记。
- [x] Redis 未配置、断开和恢复场景均有自动化覆盖。
- [x] Redis 降级期间 SSE 使用有界周期刷新，不会无限期保留初始行情。
- [x] Redis 降级日志已脱敏，并可区分共享标记与本地回退状态。
- [x] 既有 SSE access cookie 鉴权、单用户连接、全局连接、50 个 symbol 上限和心跳契约无回归。
- [x] Compose 中 backend 与 worker 共享一致的 Redis 和 SSE 部署模式配置，scheduler owner 仍唯一。
- [x] R7 专项测试、现有 SSE/scheduler/生产配置测试、后端全量 pytest 和 Ruff 全部通过。
- [x] Backend CI 执行 R7 门禁并成功，远程证据链接已写入发布记录。
- [x] `CHANGELOG.md`、`AGENTS.md`、`.agents/`、README、迭代计划和发布清单已同步更新。
- [x] `python/docs/sse_scaling_strategy.md` 准确区分共享更新标记与尚未实现的分布式连接注册。
- [x] 新增 R7 发布记录，明确其为工程基线或发布候选，不表述为真实生产已发布。
- [x] 文档链接、测试计数、提交哈希、回滚点和未完成生产项已完成一致性检查。
- [x] 最终提交仅包含 R7 相关变更，pre-commit 和 `git diff --check` 通过。
- [x] 迭代提交已推送至 GitHub `origin/master`，本地与远程提交一致且工作区干净。
